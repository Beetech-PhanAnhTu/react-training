const innitState = {
    message: '',
    messages: []
}

const reducer = (state, action) => {
    switch(action.type){
        
    }

    return state;
}

export { innitState }; 
export default reducer;